apiVersion: v1
kind: Service
metadata:
  name: accounts-db
  labels:
    application: bank-of-anthos
    component: accounts-db
spec:
  ports:
  - name: tcp
    port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    application: bank-of-anthos
    component: accounts-db
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-db
  labels:
    application: bank-of-anthos
    component: accounts-db
spec:
  replicas: 1
  selector:
    matchLabels:
      application: bank-of-anthos
      component: accounts-db
  template:
    metadata:
      labels:
        application: bank-of-anthos
        component: accounts-db
    spec:
      containers:
      - name: accounts-db
        image: mdimonte/accounts-db:v0.6.3
        envFrom:
        - configMapRef:
            name: environment-config
        - configMapRef:
            name: accounts-db-config
        - configMapRef:
            name: demo-data-config
        ports:
        - containerPort: 5432
        resources:
          limits:
            cpu: 250m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
        volumeMounts:
        - mountPath: /var/lib/postgresql/data
          name: db
          subPath: postgres
      volumes:
      - name: db
        persistentVolumeClaim:
          claimName: account-db
